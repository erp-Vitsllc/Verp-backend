export const CARD_DELETED_PROGRESS_TYPE = "Card Deleted Progress";

/**
 * Card-delete progress is surfaced via on-page toast only — no dashboard bell row.
 */
export async function notifyCardDeletedProgressUpdated() {
    return;
}
